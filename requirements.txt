# Perseus Memory Agent — dependencies
# Minimum for standalone use with Engram-rs backend.
# For Elastic backend, Google Cloud Agent Builder handles the MCP connection.

# Core (no heavy deps needed — Agent Builder provides Gemini + MCP)
pydantic>=2.0
python-dotenv>=1.0

# Shared memory + tool layer (extracted from this repo and perseus-rapid-agent)
perseus-agent-core>=0.2.0

# Elastic backend
elasticsearch>=8.0

# Async support
anyio>=4.0
